name: Build ZMK firmware

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/zmkfirmware/zmk-dev-arm:latest

    steps:
      - name: Checkout config repo
        uses: actions/checkout@v3

      - name: Initialize west workspace from config/
        run: west init -l config

      - name: Update modules
        run: west update

      - name: Remove conflicting shield widget
        run: |
          if [ -f zmk/app/boards/shields/nice_view/widgets/status.c ]; then
            rm zmk/app/boards/shields/nice_view/widgets/status.c
          fi

      - name: Build firmware
        run: |
          west build -s zmk/app -b nice_nano_v2 \
            -- -DSHIELD=nice_view \
               -DZMK_CONFIG=${{ github.workspace }}
